/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SideDrawerTriggerDirective } from './side-drawer-trigger.directive';

describe('Directive: SideDrawerTrigger', () => {
  it('should create an instance', () => {
    const directive = new SideDrawerTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
